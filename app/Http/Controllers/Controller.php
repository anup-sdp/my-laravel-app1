<?php
// A controller normally handles the request and calls models/services/etc. (similar django views). 
// This is a base controller class that other controllers can extend. It doesn't have any functionality on its own, 
// but it can be used to share common logic or middleware between controllers.

namespace App\Http\Controllers;

abstract class Controller
{
    //
}
